apiVersion: v1
kind: Service
metadata:
  name: sync
  annotations:
    service.beta.kubernetes.io/azure-load-balancer-internal: "true"
spec:
  selector:
    app: sync
  ports:
    - port: 3004
      targetPort: 3004
  type: LoadBalancer